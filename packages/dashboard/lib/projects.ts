// Re-export everything from the projects package
// This wrapper is needed because Next.js has issues resolving
// packages that are outside of the dashboard's directory structure
export * from '@vibe-kit/projects';