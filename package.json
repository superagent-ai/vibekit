{
    "name": "vibekit-workspace",
    "private": true,
    "type": "module",
    "version": "1.0.0",
    "engines": {
        "node": ">=18.0.0"
    },
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "npm run build:packages",
        "build:packages": "npm run build --workspace=packages/core && npm run build --workspace=packages/e2b && npm run build --workspace=packages/daytona && npm run build --workspace=packages/cloudflare && npm run build --workspace=packages/modal && npm run build --workspace=packages/beam && npm run build --workspace=packages/blaxel",
        "dev": "npm run dev --workspace=packages/core",
        "type-check": "npm run type-check --workspaces",
        "test": "vitest"
    },
    "devDependencies": {
        "@types/node": "^22.15.18",
        "@vitest/coverage-v8": "^3.1.3",
        "@vitest/ui": "^3.1.3",
        "tsup": "^8.4.0",
        "typescript": "^5.8.3",
        "vitest": "^3.1.3"
    },
    "dependencies": {
        "dotenv": "^17.2.0",
        "modal": "^0.3.21"
    }
}
