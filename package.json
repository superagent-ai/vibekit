{
  "name": "vibekit-workspace",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "vibekit": "./packages/cli/dist/index.js"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "build:packages": "npm run build --workspace=packages/db && npm run build --workspace=packages/e2b && npm run build --workspace=packages/daytona && npm run build --workspace=packages/northflank && npm run build --workspace=packages/cloudflare && npm run build --workspace=packages/dagger && npm run build --workspace=packages/auth && npm run build --workspace=packages/telemetry && npm run build --workspace=packages/vibekit && npm run build --workspace=packages/cli",
    "dev": "npm run dev --workspace=packages/vibekit",
    "type-check": "npm run build --workspace=packages/db && npm run build:types --workspace=packages/dagger && npm run build --workspace=packages/telemetry && npm run type-check --workspaces",
    "test": "vitest && vitest run -c vitest.integration.config.ts",
    "test:unit": "vitest",
    "test:integration": "vitest run -c vitest.integration.config.ts",
    "benchmark": "tsx scripts/run-benchmarks.ts",
    "benchmark:quick": "tsx scripts/run-benchmarks.ts quick",
    "benchmark:stress": "tsx scripts/run-benchmarks.ts stress",
    "benchmark:production": "tsx scripts/run-benchmarks.ts production",
    "benchmark:all": "tsx scripts/run-benchmarks.ts all",
    "benchmark:help": "tsx scripts/run-benchmarks.ts help"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@dagger.io/dagger": "^0.18.14",
    "chokidar": "^4.0.3",
    "dotenv": "^17.2.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.18",
    "@vitest/coverage-v8": "^3.1.3",
    "tsup": "^8.4.0",
    "tsx": "^4.7.0",
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "overrides": {
    "esbuild": "^0.25.0"
  }
}
